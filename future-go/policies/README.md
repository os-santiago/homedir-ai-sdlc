# Policy bundles

Defaults are safe organization-wide constraints. Repository-specific policy
must live in the managed repository under `.ai-sdlc/` and may tighten, but not
weaken, these defaults.

Policy changes are reviewed and released separately from service binaries.
Every decision records the exact policy bundle version used.
